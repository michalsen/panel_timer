<?php
/**
 * @file
 * Panel Content Timer
 *
 */

/**
 * Implements ctools_plugin_directory()
 */
function panel_timer_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && !empty($plugin)) {
    return 'plugins/' . $plugin;
  }
}